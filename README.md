Here is my Emacs configuration. I strives to keep it *simple* as much as possible but there are still self-made crutches. Mostly I use Emacs to write notes and articles using [Org Mode](https://orgmode.org/).

Directory structure:

-   `loadable`{.verbatim} - directory for standalone packages those are not available in `MEPLA`{.verbatim}
